# FraudGuard AI — Backend Environment Variables
# Copy this to .env

SECRET_KEY=change-this-to-a-long-random-string-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

DATABASE_URL=postgresql://neondb_owner:npg_mGh6NUHBDl4K@ep-square-shadow-a1gig8w2-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173"]

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@fraudguard.ai

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

REDIS_URL=redis://localhost:6379

# IMPORTANT: these paths are resolved relative to the backend/ folder
# Do NOT add leading slashes or change the folder names
MODEL_PATH=ml_pipeline/models/fraud_model.joblib
SCALER_PATH=ml_pipeline/models/scaler.joblib

# Fraud thresholds — FIXED (was 0.30/0.70, now 0.30/0.60)
LOW_RISK_THRESHOLD=0.30
HIGH_RISK_THRESHOLD=0.60
OTP_EXPIRE_SECONDS=300

